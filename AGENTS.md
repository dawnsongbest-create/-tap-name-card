# AGENTS.md

## 1. 项目目标

本项目是一个微信小程序 MVP，帮助用户创建高度视觉化的个人名牌，并在线下兴趣活动、朋友聚会和社交场景中，通过微信分享、小程序码，以及未来的 NFC 贴片完成自我介绍与破冰。

产品不是企业电子名片，不是销售 CRM，也不是陌生人推荐、附近的人或恋爱匹配平台。

项目唯一正式产品需求来源：

`/docs/PRD.md`

当代码、其他文档与 PRD 冲突时，以 `/docs/PRD.md` 中明确且最新的需求为准。

## 2. 当前阶段

当前阶段为小规模可用 MVP。

优先级：

1. 核心流程真实可用
2. 用户数据与隐私安全
3. iPhone 与 Android 真机体验
4. 代码清晰、可维护
5. 为未来 NFC 能力保留扩展空间

不要为尚未出现的大规模用户提前引入复杂架构。

## 3. 默认技术方向

除非 PRD 或 `/docs/DECISIONS.md` 明确修改，否则采用：

- 微信原生小程序
- TypeScript
- WXML
- WXSS
- 微信云开发或 CloudBase
- 云数据库
- 云存储
- 云函数
- Canvas 2D 生成分享图片

不要擅自改用：

- Taro
- uni-app
- React 小程序转换框架
- 独立 NestJS 服务
- PostgreSQL
- Redis
- Kubernetes
- 微服务架构

若默认技术方向无法满足需求，先写入 `/docs/DECISIONS.md`，说明问题、备选方案和影响，不得直接更换。

## 4. 禁止自行增加的功能

不得自行增加：

- 附近的人
- 陌生人推荐
- 滑动匹配
- 恋爱匹配
- 公开关注、粉丝和好友数量
- 动态广场或内容推荐流
- 点赞、公开评论和排行榜
- 站内聊天
- 访客追踪
- 企业管理或销售 CRM
- 广告、付费会员或模板商城
- 自动添加微信好友
- 实时位置共享
- 完整自由画布编辑器

## 5. 关键产品约束

必须保证：

- 未登录用户可以浏览公开名牌
- 打开名牌不会通知名牌主人
- 头像不是社交名牌必填项
- 用户先选模板，再编辑内容
- 编辑方式为表单填写并实时预览
- 名牌内容模块可以自由组合
- AI 内容必须由用户确认后才能写入
- 收藏与认识请求是两个独立操作
- 收藏不会通知名牌主人
- 认识请求必须让名牌主人明确知情
- 发起认识请求前，用户必须确认递出的名牌
- 名牌主人只能通过“接受并回赠名牌”接受请求
- 双方完成回赠后才能创建相遇
- 双方完成回赠后才能申请交换联系方式
- 联系方式必须经过对方确认后才可见
- 不显示请求已读状态
- 不公开粉丝数、好友数、收藏数或人气指标
- 历史相遇保存当时的名牌快照

## 6. 工作方式

每次执行任务前：

1. 阅读本文件
2. 阅读 `/docs/PRD.md`
3. 阅读当前任务相关的工程文档
4. 总结本次任务范围
5. 列出计划修改的文件
6. 检查阻塞问题
7. 明确本次不实现的内容

每次只完成一个明确里程碑。未经用户明确要求，不要一次实现整个产品。

## 7. 不确定信息处理

不得虚构：

- 微信小程序 API
- 微信登录、分享和小程序码能力
- 内容安全审核能力
- NFC 唤起能力
- CloudBase 接口
- 用户授权行为

无法确认的平台能力必须查阅官方文档，并将结论写入 `/docs/DECISIONS.md`。需要为不可用或失败情况提供兜底方案。

## 8. 数据与权限

所有关键写操作必须通过云函数完成。

客户端不得直接决定：

- 名牌发布状态
- 认识请求状态
- 相遇关系
- 联系方式交换状态
- 拉黑关系
- NFC 绑定关系

所有云函数必须：

- 校验用户身份
- 校验资源所有权
- 校验输入
- 防止重复提交
- 返回统一错误格式
- 不暴露 OpenID、密钥和内部错误

## 9. 页面状态

每个页面和核心组件必须包含：

- 正常状态
- 加载状态
- 空状态
- 网络失败状态
- 权限不足状态
- 内容不存在状态
- 重试入口

不得只实现成功路径。

## 10. 完成要求

每个任务完成后必须：

1. 运行格式检查
2. 运行类型检查
3. 运行已有测试
4. 为新增核心逻辑补充测试
5. 检查是否修改无关功能
6. 更新 `/docs/TASKS.md`
7. 更新必要的工程文档
8. 汇报已完成内容
9. 汇报真实测试结果
10. 列出仍未完成或无法验证的内容

不得声称未实际运行的测试已经通过。

## 11. 文档维护

仓库需要持续维护：

- `/docs/MVP_SCOPE.md`
- `/docs/ARCHITECTURE.md`
- `/docs/DATA_MODEL.md`
- `/docs/API_SPEC.md`
- `/docs/UI_SPEC.md`
- `/docs/TEST_PLAN.md`
- `/docs/TASKS.md`
- `/docs/DECISIONS.md`

`TASKS.md` 必须反映真实进度。`DECISIONS.md` 用于记录已确认的产品和技术决策。
